'use server';

export const signIn = async () => {
    try{

    } catch(error) {
        console.error('Error',error)
    }
}

export const signUn = async (userDate: SignUpParams) => {
    try{

    } catch(error) {
        console.error('Error',error)
    }
}